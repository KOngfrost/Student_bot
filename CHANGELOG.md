# История изменений (Changelog)

Все ключевые изменения проекта документируются в этом файле.  
Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/), проект придерживается [семантического версионирования (SemVer)](https://semver.org/).

## [0.8.3.16] — 2026-09-24

### Изменено и улучшено
- **Полная переработка Telegram-бота мониторинга (архитектура с нуля)**:
  - Модульная структура в `bots/telegram/`:
    - `bots/telegram/keyboards.py`: компоненты Reply и Inline клавиатур с нативной поддержкой кнопок Telegram Mini App (`WebAppInfo`).
    - `bots/telegram/handlers/`: специализированные роутеры для базовых команд (`base.py`), статуса сервера (`status.py`), техработ (`maintenance.py`), двухфакторной аутентификации (`two_factor.py`), управления контейнерами (`containers.py`) и резервного копирования (`backup.py`).
    - `bots/telegram/bot.py`: точка сборки и фабрика бота с обратной совместимостью для тестов и внешних модулей.
- **Поддержка Telegram Mini App (TMA) через DuckDNS**:
  - Интегрирован домен `yenotick.duckdns.org` (настраиваемый параметр `TELEGRAM_WEBAPP_URL`).
  - Постоянная кнопка меню чата Telegram (`MenuButtonWebApp`) для запуска веб-панели в 1 клик рядом со строкой ввода.
  - Кнопка `📱 Веб-панель` на главной клавиатуре и в карточках команд `/start`, `/help`, `/status`, `/panel`.
  - `web/security/middleware.py`: обновлены CSP и заголовки фреймов (`frame-ancestors 'self' https://*.telegram.org https://telegram.org;`), позволяющие Telegram Web и WebView встраивать веб-панель без блокировок.
  - `web/templates/base.html`: подключен Telegram WebApp JS SDK с автоматическим разворачиванием окна (`ready()`, `expand()`).
  - `Caddyfile` & `docker-compose.yml`: добавлен сервис Caddy с автоматическим получением и продлением Let's Encrypt SSL-сертификатов на портах 80/443 для `yenotick.duckdns.org`.
- **Очистка и регистрация меню слэш-команд**:
  - `setup_bot_commands`: автоматический сброс старых команд из кэша Telegram (`delete_my_commands`) и регистрация строгого набора команд (`status`, `panel`, `restart`, `logs`, `backup`, `maintenance`, `2fa`, `help`) с русскоязычными описаниями в области чата администратора.

---

## [0.8.3.15] — 2026-09-24

### Добавлено
- **Динамическое отключение и включение двухфакторной аутентификации (2FA)**:
  - `core/two_factor.py`: создан сервис централизованного управления состоянием 2FA (`is_two_factor_enabled`, `set_two_factor_mode`, `get_two_factor_info`) с сохранением в Redis (`oss_bot:system:2fa_enabled`), локальном дисковом кэше (`oss_bot_2fa_state.json`) и оперативной памяти для гарантированной устойчивости к перезапускам.
  - `web/routes/auth.py`: динамическая проверка 2FA в процедуре входа администратора и подтверждении bootstrap. При выключенной 2FA вход осуществляется по связке логин/пароль без отправки одноразового кода в VK и без перенаправления на шаг `/auth/2fa`.
  - `web/routes/settings.py` & `web/templates/settings.html`: в настройках системы для роли `SUPERADMIN` добавлена карточка управления 2FA с текущим статусом (🟢 Включена / ⚪ Отключена) и возможностью безопасного переключения с подтверждением.
  - `web/routes/api_v1.py`: эндпоинт системных метрик возвращает актуальное динамическое состояние 2FA.
  - `bots/telegram/bot.py`: добавлена команда `/2fa`, кнопка в главном меню `🔐 2FA`, инлайн-клавиатура для быстрого включения/отключения 2FA главным администратором в случае экстренных ситуаций, статус отображается в отчёте `/status`.
  - `tests/test_two_factor_toggle.py`: полный набор модульных тестов на переключение состояния, сохранение и проверку прав доступа.

---

## [0.8.3.14] — 2026-09-24

### Изменено и улучшено
- **Типографика, внутренние и внешние отступы карточек веб-панели**:
  - `web/static/style.css`:
    - Карточки `.card` получили просторные внутренние отступы `26px 28px` (вместо `20px`), увеличенный внешний отступ `margin-bottom: 28px` и заголовки `18px` с межстрочным интервалом `1.35`.
    - Карточки статистики `.stat-card` увеличены: внутренние отступы `22px 24px`, размер числовых значений поднят с `28px` до `32px`, подписи — `13.5px`, сетка `.stats-grid` получила шаг `gap: 20px`.
    - Модальное окно чата/диалога `.modal-ticket-lg`: ширина увеличена до `880px`, внутренний паддинг — `28px 32px`.
    - Сводка заявки `.ticket-header-summary`: просторные ячейки `minmax(210px, 1fr)`, паддинг `18px 22px`, отступы `gap: 16px 20px`, контрастные и читаемые подписи `12px` (uppercase) и значения `14.5px`.
    - Область диалога `.ticket-chat-container`: высота расширена до `480px`, внутренние поля `22px 24px`, закругления баблов `16px`, размер шрифта сообщений `14.5px` с интервалом `1.55`.
    - Поле ввода ответа `.chat-input-container`: увеличенная текстовая область (min-height `84px`, шрифт `14.5px`), эргономичные кнопки и подсказки.
    - Карточки администраторов `.admin-card` и отделов `.department-card`: увеличены внутренние отступы до `20px 22px` и `24px 26px`, метрики отделов подняты до `19px`.
    - Таблицы данных: паддинг строк `13px 16px`, шрифты заголовков `12px`, ячеек `14px`.
    - Элементы форм и селекты: оптимизированы внутренние поля `10px 14px`, шрифт `14px`.
    - Добавлены адаптивные правила в `@media (max-width: 600px)` для комфортного отображения модального окна и диалога на смартфонах.
  - `web/templates/tickets.html`:
    - Удалены сжатые инлайн-стили (`font-size: 12.5px; padding: 4px 8px`), заменены на семантические CSS-классы `.ticket-action-form`, `.ticket-action-label`, `.ticket-action-select`.

---

## [0.8.3.13] — 2026-09-24

### Добавлено
- **История диалога в веб-панели (Messenger UI)**:
  - `web/static/style.css`: полноценные стили чата (`.ticket-chat-container`, `.chat-bubble`, `.chat-bubble-user`, `.chat-bubble-admin`, `.chat-bubble-system`), плавная прокрутка к последнему сообщению, адаптивная карточка заявки (`.modal-ticket-lg`).
  - `web/templates/tickets.html`: интерактивное модальное окно переписки со студентом, кнопки быстрых шаблонов ответов («Номер комнаты», «Принято в работу», «Коменданту», «Работы выполнены»), отправка ответа без перезагрузки всей страницы (AJAX) и поддержка горячей клавиши `Ctrl + Enter`.
  - `web/routes/tickets.py`: поддержка AJAX-ответов (возврат JSON с сохранением в истории `ticket_messages` и доставкой через VK outbox) и поле `created_at_display` для каждого сообщения в таймзоне проекта.
- **Интерактивное решение вопросов администратором в VK-боте**:
  - `bots/vk/common.py`: добавлен FSM StateGroup `AdminTicketStates` (`WAITING_REPLY_TEXT`) для пошагового ответа на обращения через личные сообщения VK.
  - `bots/vk/keyboards.py`: добавлены клавиатуры списка активных заявок (`build_admin_tickets_list_keyboard`), карточки действий над заявкой (`build_admin_ticket_actions_keyboard`) и клавиатура отмены ввода ответа (`build_admin_reply_cancel_keyboard`).
  - `bots/vk/handlers/admin.py`: реализованы хендлеры `admin_ticket_view_handler` (`Заявка #N`), `admin_reply_start_handler` (`Ответить #N`), `admin_reply_text_handler` (приём текста ответа админа), `admin_ticket_history_handler` (`История #N`), а также быстрое завершение (`Выполнено #N`) и перевод в статус «В обработке» (`В обработку #N`).
  - `core/ticket_service.py`: добавлена автоматическая фоновая доставка оповещений о новых заявках администраторам соответствующего отдела через outbox VK.

---

## [0.8.3.12] — 2026-09-24

### Добавлено
- **Централизованное управление версиями**:
  - `core/__init__.py`, `core/config.py`: переменная `APP_VERSION` в `.env` теперь является единым источником версионности для ядра, API, шаблонов и ассетов.
- **Режим технических работ (Maintenance Mode)**:
  - `core/maintenance.py`: распределённый сервис контроля статуса техработ на базе Redis (AOF) с in-memory кэшированием и файловым fallback.
  - `web/templates/maintenance.html`: современная страница обслуживания (HTTP 503) с поддержкой тёмной и светлой темы оформления, анимацией и индикатором версии.
  - `web/security/middleware.py`, `web/main.py`: `MaintenanceMiddleware` перехватывает запросы к веб-панели при активных техработах, возвращая страницу 503 (или JSON для API). Авторизованным суперадминистраторам открыт доступ для проведения обслуживания с предупреждающим баннером.
  - `bots/vk/bot.py`: `VKMaintenanceMiddleware` перехватывает все входящие сообщения пользователей, отправляя автоответ о проведении технических работ.
  - `bots/telegram/bot.py`: управление режимом техработ исключительно через Telegram-бота главного администратора (кнопка `🚧 Техработы`, команды `/maintenance` и `/maint`, интерактивные inline-кнопки включения/отключения с мгновенным обновлением).

---

## [0.8.3.11] — 2026-09-24

### Исправлено
- **VK Бот — сопоставление глобальных и локальных номеров заявок**:
  - `bots/vk/handlers/student.py`: поддержан поиск заявки студента сначала по глобальному номеру `Ticket.id` (который бот присылает при создании и в ответах оператора), с fallback на локальный порядковый номер в списке последних заявок. Устранена ошибка «Заявка не найдена» и отправка ответа не в тот тикет.
  - `core/ticket_service.py`: добавлена функция `get_user_ticket_by_id(vk_id, ticket_id)` с проверкой прав владения тикетом.
- **PgBouncer интеграция**:
  - `docker-compose.yml`: сервисы `bot` и `web-admin` переведены на подключение через `pgbouncer:6432` (`migrate` сохранён на прямой порт `db:5432`).
  - `core/config.py`: свойства `db_host` и `db_port` автоматически возвращают `pgbouncer:6432` при включенном `DB_USE_PGBOUNCER=true`.
- **Rate limiting за прокси и mesh-сетью Tailscale**:
  - `web/routes/auth.py`: добавлена функция `is_trusted_proxy` с поддержкой проверки CIDR-подсетей и loopback-адресов. Заголовок `X-Forwarded-For` корректно обрабатывается от доверенных прокси, устраняя риск общей блокировки всех администраторов.
- **Telegram Monitor — безопасность и управление**:
  - `bots/telegram/bot.py`: команда `/reboot` переведена на корректный и безопасный перезапуск всех контейнеров проекта с уведомлением о необходимости SSH для перезагрузки хоста.
  - `Dockerfile.monitor`: добавлен непривилегированный пользователь `appuser` (UID 1000) и группа `docker` (GID 988), устраняющие запуск контейнера из-под `root`.
- **Планировщик ежедневных отчётов**:
  - `core/reporting.py`: добавлен механизм повторных попыток отправки отчёта (с интервалом 5 минут) при кратковременных сбоях VK API/SMTP в 09:00, а также автоматическая отправка неотправленного суточного отчёта при рестарте сервиса после часа X.

---

## [0.8.3.10] — 2026-09-24

### Исправлено
- **VK LongPoll отказоустойчивость и автовосстановление**:
  - Реализован кастомный `RobustBotPolling`, устраняющий зависание бота при ошибках сессии VK API (`VKAPIError_3: Not found` и др.).
  - Автоматический сброс кэшированного сервера (`server = None`) при 3+ повторных ошибках LongPoll вместо бесконечного цикла `Unable to make request to BotPolling, retrying...` с протухшим ключом.
  - Добавлены ретраи с экспоненциальным backoff в `get_server()` для методов `groups.getById` и `groups.getLongPollServer`.
  - Привязка `touch_heartbeat()` непосредственно к циклу опроса LongPoll, что позволяет docker healthcheck мгновенно выявлять зависание сети.

---

## [0.8.3.9] — 2026-09-19

### Исправлено и улучшено
- **Комплексный аудит и исправление системных багов**:
  - `core/reporting.py`: исправлено переполнение `random_id` (диапазон `2**31 - 1`), актуализирован docstring `REPORT_CHUNK_SIZE`.
  - `core/database.py`: добавлен lazy guard для `async_session_maker` при `engine is None`.
  - `core/cache.py`: устранено задвоение счётчика удалённых ключей в `cache_delete_pattern`.
  - `core/outbox.py`, `core/config.py`: параметры очереди outbox перенесены в Pydantic `Settings`.
  - `core/bulk_import.py`: добавлена санитизация Excel-ячеек `_sanitize_excel_cell()` от Formula/CSV Injection.
  - `core/ticket_service.py`: очищен избыточный `tuple()` в фильтрах SQLAlchemy.
  - `bots/vk/handlers/faq.py`: исправлен regex FAQ-хендлера, исключён ложный перехват произвольных чисел.
  - `bots/vk/handlers/reports.py`: добавлены fallback-хендлеры FSM со сбросом состояния при ошибках ввода даты; добавлено уведомление об отсутствии прав админа.
  - `bots/vk/handlers/student.py`, `bots/vk/keyboards.py`: добавлена клавиатура с кнопкой «Создать заявку» при подсказке из базы знаний.
  - `bots/telegram/`: таймаут клиента увеличен до 120с (`docker_client.py`), добавлена проверка наличия бинарников перезагрузки (`shutil.which`) в `/reboot`, учтён лимит 50 МБ Telegram Bot API для бэкапов.
  - `web/routes/settings.py`: синхронизировано имя cookie оформления (`app_glass_effect`).
  - `docker-compose.yml`: сокет Docker переведён в read-only (`:ro`), исправлен healthcheck сервиса `tg-monitor` на проверку через Python, добавлены флаги `--no-auth-warning`.

---

## [0.8.3.5] — 2026-09-18

### Исправлено и улучшено
- **Docker Compose & DevOps**:
  - Настроены жесткие ограничения оперативной памяти (`mem_limit` и `memswap_limit`) для всех 8 сервисов для предотвращения OOM Killer.
  - Добавлены healthcheck-проверки для сервисов `prometheus` и `tg-monitor`.
- **Конфигурация & Безопасность**:
  - `core/config.py`: убран `extra="allow"` в пользу `extra="ignore"`, чтобы опечатки в `.env` не попадали в модель настроек.
  - Устранен баг переопределения `REPORT_EMAILS` на уровне класса через прямой `os.getenv()`, обходивший валидаторы Pydantic.
- **Оптимизация кода**:
  - `core/ticket_service.py`: удален дублирующий словарь `STATUS_LABELS` (используется прямой `TicketStatus.value`), удален устаревший метод `create_anonymous_ticket`.
  - `bots/vk/handlers/admin.py`: парсинг статусов переведен на прямой перебор enum `TicketStatus`.
  - `core/models.py`: поле `FAQNode.depth` аннотировано как `ClassVar[int]`, исключая конфликт с маппингом SQLAlchemy.
- **Интерфейс & Ассеты**:
  - Синхронизированы версии ассетов и бейджи веб-панели до `v0.8.3.5`.

---

## [0.8.3] — 2026-09-16

### Добавлено
- **Telegram-бот мониторинга и Devops-управления для главного администратора**:
  - Строгий контроль доступа: бот отвечает исключительно пользователю с `TELEGRAM_ADMIN_ID` из `.env`.
  - Мониторинг ресурсов сервера: загрузка CPU, памяти RAM, свободного места на диске и времени аптайма с наглядными шкалами.
  - Состояние Docker-контейнеров: статус, здоровье (healthy/unhealthy), вывод карточки по команде `/status`.
  - Автоматические алерты при сбоях: мгновенное уведомление в Telegram при переходе контейнера в `unhealthy` или неожиданном падении, уведомление при восстановлении сервиса, защита от повторного спама.
  - Интерактивное управление через инлайн-кнопки:
    - Перезапуск отдельных контейнеров или всего стека в один клик (`/restart`).
    - Просмотр последних 35 строк логов любого контейнера (`/logs`).
    - Безопасная перезагрузка физического сервера с подтверждением (`/reboot`).
  - Сервис `tg-monitor` в `docker-compose.yml` с прямым доступом к Docker Engine API через `/var/run/docker.sock`.

---

## [0.8.2] — 2026-09-16

### Добавлено
- **Модуль мероприятий и регистрации**:
  - Фильтрация актуальных событий по времени проведения (`event_date >= now - 3h`).
  - Поддержка описания мероприятий в ответах бота и синонимы команд («Мероприятия», «События», «Афиша», `/events`).
  - Добавлен пинг heartbeat при вызове событийных команд и юнит-тесты.

### Изменено
- **Интерфейс веб-панели**:
  - Обновлён дизайн карточек и элементов управления (стиль Glassmorphism с матовым размытием `backdrop-filter: blur(14px)`).
  - Сбалансированная цветовая схема в светлой теме с мягкими акцентами.
  - Удалены лишние эмодзи из служебных заголовков, скриптов и веб-интерфейса для строгого делового стиля.
  - Обновлён сайдбар: сквиркл-аватары с первой буквой имени, векторные SVG-иконки, отображение роли оператора.

### Исправлено
- **Двухфакторная аутентификация (2FA)**:
  - Устранён дедлок автоотправки формы входа при вводе 6 цифр (зависание в статусе «Проверка...»).
  - Корректная обработка `401 Unauthorized` в фоновых скриптах без цикличных запросов.
  - Исправлен `AttributeError` при обращении к признаку активности событий в боте.

---

## [0.8.1] — 2026-09-15

### Безопасность
- **Startup Guard и Bootstrap Guard**:
  - Автоматическая проверка конфигурации при старте (`core/startup_guard.py`).
  - Блокировка запуска в режиме `production` при обнаружении паролей по умолчанию (`admin123`, `changeme` и т.д.).
- **Харденинг 2FA и защита от перебора**:
  - Лимит попыток ввода OTP-кода (не более 5 ошибок) и повторной отправки (не более 3 раз).
  - Хеширование одноразовых кодов с помощью Argon2id и хранение в Redis с TTL.
  - Возврат корректных HTTP-статусов `401` и `429 Too Many Requests` с заголовком `Retry-After`.

### Оптимизация и надежность
- **Ускорение поиска**: добавлены триграммные GIN-индексы PostgreSQL (`pg_trgm`) для быстрого нечёткого поиска по тикетам и базе знаний.
- **Целостность данных**: настроены внешние ключи с правилами каскадного удаления и обнуления (`ON DELETE CASCADE` / `SET NULL`).
- **Disaster Recovery**: добавлены скрипты резервного копирования и восстановления с проверкой контрольных сумм SHA-256 (`scripts/backup.ps1`, `scripts/restore.ps1`).
- **Мультиязычность (i18n)**: заложена основа локализации интерфейса и сообщений (RU/EN).

---

## [0.8.0] — 2026-09-08

### Добавлено
- **Стандартизированные Excel-отчёты**:
  - Генерация отчёта строго на 5 листов: сводная аналитика + отдельные листы по 4 отделам («Жилищно-бытовой», «Информационный», «Корпоративный», «Культурно-массовый»).
- **Модульная структура VK-бота**:
  - Разделение обработчиков на доменные пакеты `bots/vk/handlers/` на базе `BotLabeler`.
- **REST API v1**:
  - Публичные эндпоинты `/api/v1` со строгой типизацией Pydantic v2 и документацией Swagger (`/docs`).
- **Redis-инфраструктура**:
  - Распределённые сессии пользователей веб-панели (`RedisSessionMiddleware`).
  - Кэширование справочников с инвалидацией по тегам.
  - Блокировки воркеров Outbox через Redis (совместимо с PgBouncer в режиме транзакций).

### Безопасность
- Единый экран ввода 2FA с автовставкой кода и обратным отсчётом таймера.
- Ограничение доступа к журналу аудита и выгрузке логов только для `SUPERADMIN`.
- Контроль синхронизации времени между приложением и PostgreSQL на эндпоинте `/health`.

---

## [0.7.0] — 2026-08-25

### Добавлено
- Внедрение пулера соединений **PgBouncer** для снижения нагрузки на PostgreSQL при всплесках трафика.
- Метрики **Prometheus** (`/metrics`) и правила базовых алертов.
- Защита веб-панели: CSRF-токены, заголовки безопасности (CSP, X-Frame-Options), ограничение попыток входа (Rate Limiting).
- Поддержка закрытого VPN-доступа через **Tailscale**.

---

## [0.1.0 – 0.6.0] — MVP и базовая функциональность

- Реализация ядра системы на Python 3.11+ и SQLAlchemy 2.0 (asyncpg).
- Гарантированная доставка сообщений студентам через паттерн **Transactional Outbox**.
- VK-бот для приёма обращений студентов по отделам.
- Веб-панель управления на FastAPI и Jinja2 шаблонах.
- Базовые миграции структуры базы данных Alembic.
- Интеграция со скриптами создания администраторов и базовым резервным копированием.
