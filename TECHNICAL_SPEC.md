# Техническое задание: OSS Bot (MVP)

> Примечание: документ исторический — это ТЗ исходного MVP (проект ранее
> назывался Student Bot). Актуальная документация: [README.md](README.md),
> [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md), [docs/WEB_ADMIN_GUIDE.md](docs/WEB_ADMIN_GUIDE.md).

## 1. Цель и границы проекта
Создать минимально жизнеспособный продукт (MVP) для приёма и маршрутизации обращений студентов через VK с управлением через защищённую веб-панель. 
**Границы MVP:** 
- Только VK как клиентский канал.
- Только веб-панель для администраторов (без мобильных приложений).
- **Отказ от внешней отчётности:** email-рассылок. Вся аналитика доступна как в отчете, так и внутри веб-панели.
- Приоритет: стабильность ядра, надёжная доставка сообщений (Outbox) и безопасность данных.

## 2. Архитектура и компоненты
Система состоит из 4 изолированных слоёв:
1. **Bot Layer (`bots/vk`)**: Асинхронный VK-бот на `vkbottle`, отвечающий только за взаимодействие с пользователем и сбор данных через FSM (Finite State Machine).
2. **Core Layer (`core`)**: Бизнес-логика, работа с БД (SQLAlchemy), паттерн Outbox для гарантированной доставки ответов из веб-панели в VK, валидация конфигурации.
3. **Web Layer (`web`)**: Административная панель на FastAPI + Jinja2. Серверный рендеринг для скорости разработки.
4. **Data Layer**: PostgreSQL + Alembic для управления миграциями схемы.

## 3. Роли и права доступа
- **Студент**: Инициатор заявки. Может создать обращение, просмотреть историю и статус своих заявок.
- **Администратор отдела**: Видит и обрабатывает заявки только своего отдела. Может менять статус, отвечать, просматривать историю.
- **Суперадмин**: Полный доступ. Управление администраторами, просмотр всех заявок и логов действий, базовая внутренняя аналитика (количество заявок по статусам).

## 4. Минимальная схема базы данных
- `users`: `id`, `vk_id`, `full_name`, `dormitory`, `created_at`
- `departments`: `id`, `name` (Жилбыт, Культмасс, Информ, Корпоративный)
- `admins`: `id`, `user_id` (vk_id), `department_id`, `role` (ADMIN, SUPERADMIN)
- `tickets`: `id`, `user_id` (nullable для анонимных), `department_id`, `topic`, `description`, `status`, `response_text`, `is_anonymous`, `created_at`, `updated_at`
- `outbox_messages`: `id`, `ticket_id`, `payload`, `status` (pending, sent, failed), `attempts`, `created_at`
- `logs`: `id`, `admin_id`, `action`, `details`, `created_at`

## 5. Критерии готовности MVP
1. Студент проходит сценарий создания заявки, данные сохраняются в БД.
2. Администратор видит заявку в веб-панели, может изменить статус и написать ответ.
3. Ответ администратора гарантированно доставляется студенту в VK (через Outbox).
4. Веб-панель доступна только через Tailscale с принудительной авторизацией и HTTPS.
5. Код покрыт линтерами (ruff, mypy) и базовыми интеграционными тестами (pytest).
6. **Параллельные сессии**: Несколько администраторов могут работать одновременно с разных аккаунтов/браузеров без взаимного влияния.

---

## 6. Рекомендации по технологическому стеку для выхода из MVP
*Этот раздел фиксирует текущий выбор и даёт чёткие триггеры для смены технологий при масштабировании.*

| Компонент | Текущий стек (MVP) | Почему выбран | Триггер для смены / масштабирования |
| :--- | :--- | :--- | :--- |
| **Backend API** | Python 3.11+, FastAPI | Скорость разработки, встроенная валидация Pydantic, отличная асинхронность. | При нагрузке > 1000 RPS или необходимости строжайшей типизации во всём пайплайне рассмотреть **Go** или **Rust** для критических микросервисов (например, outbox-воркер). **Триггер для перехода на Go/Rust**: необходимость обработки > 5000 RPS, строгая типизация для безопасности, или требования к производительности при масштабировании > 10000 пользователей. |
| **База данных** | PostgreSQL + SQLAlchemy (async) | Надёжность, ACID, отличная поддержка JSON и асинхронных драйверов (`asyncpg`). | **Реализовано**: подключён **PgBouncer** (порт 6432, режим транзакций) и **Redis** (кэш с TTL, распределённые сессии). |
| **Frontend (Web)** | Jinja2 + HTMX + Tailwind CSS | Позволяет создать динамичный, отзывчивый UI без сложности SPA (React/Vue). Критично для скорости MVP. | Переход на **React/Vue** оправдан *только* при появлении сложной клиентской логики (drag-and-drop конструкторы, интерактивные графики аналитики в реальном времени). |
| **Bot Framework** | `vkbottle` | Легковесный, нативно асинхронный, хорошо документирован для VK API. | **Реализовано**: поддержка двух режимов — Long Poll и Callback API (вебхуки в FastAPI). |
| **Инфраструктура** | Docker Compose + Tailscale | Нулевая настройка reverse-proxy и SSL-сертификатов. Безопасный доступ к админке "из коробки". | Добавлены PgBouncer, Prometheus (сбор метрик и алертинг) и сценарии нагрузочного тестирования (Locust). |

---

## 6.1. Параллельные сессии и отказоустойчивость (Реализовано)

### Цель
Обеспечить одновременную работу нескольких администраторов с разных аккаунтов/браузеров без взаимного влияния.

### Архитектура
- **SessionMiddleware**: RedisSessionMiddleware с подписанными cookie (`secret_key` из `.env`)
- **Хранение сессий**: Распределённое хранилище Redis с настраиваемым TTL (`SESSION_TTL`)
- **CSRF-защита**: CSRFMiddleware — токен хранится в сессии, проверяется на все POST-запросы (внешние вебхуки Callback API исключены)
- **Rate limiting**: БД-хранилище (`login_attempts`) — 5 неудачных попыток за 15 минут на IP

### Порядок выполнения middleware
```
SecurityHeaders → Session → CSRF → CORS → handler
```

### Безопасность
- CSRF-защита для всех POST-запросов
- Rate limiting: 5 неудачных попыток за 15 минут на IP (хранилище — PostgreSQL)
- Security headers: X-Frame-Options, CSP, X-Content-Type-Options

### Тесты
- `tests/test_parallel_sessions.py`: тесты для проверки:
  - Входа администратора (bootstrap и web_users)
  - Изоляции сессий
  - Выхода из системы
  - Заголовков безопасности
  - Rate limiting

### Масштабирование
При переходе на несколько uvicorn-workers необходимо:
1. Ограничить `WEB_WORKERS=1` (без Redis сессии многопроцессность небезопасна)
2. Либо заменить cookie-сессию на Redis-backed SessionMiddleware

## 6.2. Многопроцессный rate limiting (Реализовано)

### Проблема
In-memory `RateLimiter` (словарь `requests: dict[str, list[float]]`) хранил состояние
в памяти каждого процесса Python. При `--workers 4` каждый воркер имел свой лимит,
что учетверяло доступные запросы и позволяло обходить защиту.

### Решение
- **DBRateLimiter** (`web/security/middleware.py`): использует таблицу `crud_attempts`
  для хранения состояния. Работает корректно при любом количестве воркеров.
- **Таблица `crud_attempts`**: создаётся миграцией `e8f9a0b1c2d3`, индексы по `(ip, action, attempted_at)`.
- **Применение**: CRUD-операции админ-панели (смена статуса, ответы, создание заявок)
  используют `DBRateLimiter` с лимитом 60 операций на IP за 5 минут.
- **Очистка и retention**: автоматическое удаление записей старше 24 часов из таблиц `crud_attempts` и `login_attempts` для предотвращения неконтролируемого разрастания БД.

## 6.3. Асинхронный SMTP (Реализовано)

### Проблема
`send_report_email` использовал синхронный `smtplib.SMTP` через `asyncio.to_thread`.
Хотя вызов не блокирует event loop полностью, использование синхронных сокетов
внутри асинхронного цикла — технический компромисс.

### Решение
- Заменён на `aiosmtplib.send()` — полностью асинхронная отправка.
- Убрана зависимость `smtplib`, добавлена `aiosmtplib==4.0.0`.
- Вызов `send_report_email` в `_run_report` больше не требует `asyncio.to_thread`.

---

## 7. Логика работы отделов (Реализуется в последнюю очередь)
*Данный блок реализуется строго после утверждения и тестирования ядра (БД, Outbox, базовая авторизация, создание тикета).*

### 7.1. Жилбыт
- **Сценарий**: Пошаговый сбор данных через FSM (ФИО → Общежитие → Тема → Описание).
- **Оптимизация**: Перед созданием тикета текст описания проверяется по простой таблице `knowledge_base` (ключевые слова). При совпадении тикет создаётся сразу со статусом `Выполнено (авто)` и ответом из БЗ.
- **Статусы**: `Новое` → `В обработке` → `Передано в Хозчасть` → `Выполнено`.

### 7.2. Культмасс
- **Сценарий**: Упрощённая анкета (ФИО → Тема → Описание). 
- **Фича**: Возможность массового ответа (рассылки) по списку отфильтрованных тикетов из веб-панели (например, "Всем, кто спрашивал про Новый год").

### 7.3. Информ
- **Сценарий**: Интерактивное FAQ-дерево. Студент выбирает варианты ответов через inline-клавиатуры VK, не создавая тикет.
- **Эскалация**: На любом шаге доступна кнопка «Связаться с администратором», которая прерывает дерево и создаёт тикет в отдел "Информ" с историей выбранных пунктов.

### 7.4. Корпоративный
- **Сценарий**: Максимально упрощённый поток. Сбор ФИО, общежития, темы и описания в 4 шага.
- **Особенность**: Нет автоответов и сложных статусных цепочек. Только `Новое` → `В обработке` → `Закрыто`.

### 7.5. Анонимное обращение (Опционально)
- **Сценарий**: Запрос только текста обращения.
- **Хранение**: `user_id = NULL`, `department_id = NULL`, статус `Анонимное`. Доступно для просмотра только роли `SUPERADMIN`.

---

## 8. Развитие системы: от MVP к релизу v0.7.8 (Enterprise Grade)

В версии v0.7.8 проект полностью вышел из стадии базового MVP и получил корпоративную архитектуру:

### 8.1. Распределённые сессии и многоворкерный режим
- Интегрирован сервис `oss_bot_redis` (`redis:7-alpine`).
- Реализован `RedisSessionMiddleware` (`web/security/session_store.py`) с бесшовным fallback на in-memory хранилище при локальной разработке.
- Веб-панель масштабируется на любое количество воркеров (`WEB_WORKERS=2+`) без риска потери сессий и рассинхронизации CSRF-токенов.
- Доставка фоновых сообщений координируется распределёнными блокировками Redis (`core/task_dispatcher.py`): атомарный `SET NX PX`, автопродление TTL и освобождение через Lua-скрипт. Redis-замок совместим с PgBouncer (`pool_mode=transaction`), в отличие от `pg_try_advisory_lock`.

### 8.2. Двухфакторная аутентификация (2FA через VK)
- Реализован надёжный двухфакторный вход для операторов панели: генерация криптостойкого 6-значного OTP-кода и мгновенная отправка в личные сообщения администратора через Outbox-очередь бота.
- Защита bootstrap-входа: автоматическая блокировка учетных данных из `.env` при регистрации постоянного суперадмина и визуальный баннер-предупреждение при временной bootstrap-сессии.

### 8.3. Распределённый кэш с TTL и инвалидацией
- Внедрён модуль `core/cache.py` для кэширования ресурсоёмких запросов (отделы, ключевые слова, FAQ, дашборд) с настраиваемым TTL (`CACHE_DEFAULT_TTL=300`).
- Поддерживается wildcard-инвалидация ключей по маске при любых операциях создания/редактирования/удаления данных.

### 8.4. Версионированный REST API v1
- Выделен роут `/api/v1/` с полной валидацией входных и выходных DTO через схемы Pydantic v2 (`web/schemas.py`).
- Интерактивная документация Swagger UI (`/docs`) и ReDoc (`/redoc`).

### 8.5. Наблюдаемость: Prometheus и Sentry SDK
- Реализован эндпоинт метрик Prometheus (`/metrics`) для сбора статистики запросов, задержек и системных событий.
- Централизованный сбор исключений через Sentry SDK (`core/sentry.py`, `core/bot_core.py`, `web/main.py`).

### 8.6. Редизайн веб-панели и UX
- Дизайн-система Dark Graphite + Neon Pink (`#ff4d9d`).
- Моментальный живой поиск по таблицам на стороне клиента, интерактивные фильтры по статусам с динамическими счётчиками, модальные окна для детального просмотра и ответов на обращения.
- Устранена проблема N+1 запросов при сопоставлении ключевых слов отделов за счёт пакетной загрузки в память.

### 8.7. Дружелюбная обработка ошибок
- Пользователям и администраторам выводится понятная рекомендация: *"Произошла ошибка. Пожалуйста, сфотографируйте экран и отправьте техническому администратору"* вместе с уникальным `error_id` для мгновенной локализации в логах и Sentry.

### 8.8. Релиз v0.7.9.5: Соответствие законодательству РФ (152-ФЗ), цифровая доступность и редизайн
- **Правовая база (152-ФЗ)**:
  - Реализованы публичные страницы политики конфиденциальности (`/legal/privacy`), согласия на обработку персональных данных (`/legal/consent`) и правил сервиса (`/legal/terms`).
  - Разработан плавающий компактный баннер согласия с cookie (`cookie-banner` pill-dock) с локальным сохранением выбора в `localStorage` и неоновым акцентом.
- **Цифровая доступность (WCAG 2.1 / ГОСТ Р 52872)**:
  - Корректная семантика ориентиров: `<main>`, `<nav>`, `<aside>`, `<header>`, `<footer>`.
  - Механизм быстрого пропуска навигации (`skip-link`) для скринридеров и работы с клавиатуры.
  - Контрастные фокусные кольца `:focus-visible`, корректные ARIA-атрибуты (`role="alert"`, `aria-live`, `aria-label`).
  - Обязательный содержательный `alt`-текст для всех графических элементов и монограмм.
- **SEO и мета-теги**:
  - Уникальные `meta title`, `meta description` и канонические ссылки (`canonical_url`) на всех маршрутах.
  - Полный комплект векторных SVG и растровых фавиконок (ico, svg, png-32, png-16, apple-touch-icon, squircle).
- **Эстетический редизайн UI (v0.7.9.5)**:
  - Утончённый прозрачный футер с мягким разделителем, неоновыми розовыми эффектами при наведении и аккуратным бейджем версии `v0.7.9.5`.
  - Вертикальное центрирование формы авторизации без лишней прокрутки страницы на любых экранах.
  - Автоматизированное тестирование доступности, форм и целостности ссылок (`pytest`).

### 8.9. Релиз v0.7.9.9: Модульная доменная архитектура бота, Pydantic Settings, оптимизация производительности и пентестинг
- **Модуляризация VK-бота (Domain-Driven Handlers)**:
  - Монолитный файл bots/vk/bot.py (>1000 строк) декомпозирован на независимые доменные модули в пакете bots/vk/handlers/ на базе BotLabeler: student.py, admin.py, faq.py, events.py, knowledge.py, reports.py.
  - Общий функционал и FSM-состояния вынесены в bots/vk/common.py.
  - Обеспечен O(1) поиск заявок по локальным индексам для студентов.
  - Сохранена 100% обратная совместимость всех публичных импортов и хендлеров.
- **Миграция конфигурации на Pydantic BaseSettings**:
  - Settings в core/config.py переведен на pydantic_settings.BaseSettings со строгой валидацией типов, поддержкой AliasChoices и безопасным парсингом списков из .env.
  - Реализованы специальные методы __setattr__ и __getattr__ для поддержки тестирования и дефолтных значений.
- **Комплексная безопасность и пентестинг (Security & Penetration Testing)**:
  - Разработан тестовый модуль tests/test_bot_security_and_injection.py с покрытием векторов атак: SQL-инъекции (SQLi), межсайтовый скриптинг (XSS/HTML), переполнение буфера (>65k символов), Null-байты (\\x00) и несанкционированный межведомственный доступ (IDOR/BOLA).
  - Белый список таблиц ALLOWED_TABLES в DBRateLimiter для предотвращения SQLi в ORDER BY и LIMIT.
  - Замена псевдослучайного random.randint на криптографически стойкий secrets.randbelow в core/vk_client.py.
  - Замена устаревшей библиотеки bleach на встроенный html.escape с очисткой управляющих символов.
- **Оптимизация производительности**:
  - Outbox-доставка переведена на двухфазный батчинг с ровно 2 сессиями БД вместо 1+2N.
  - In-memory кэш оптимизирован интервальной очисткой (60 с) для устранения O(N) блокировок.

### 8.10. Релиз v0.8.0: 5-страничный Excel-отчёт по отделам БД, новый экран 2FA, аудит суперадмина, синхронизация времени и дорожная карта (Android, TG Bot, TMA)
- **Стандартизированный 5-страничный Excel-отчёт**:
  - В `core/reporting.py` структура отчёта приведена строго к 5 листам: Лист 1 («Краткая информация») со сводной таблицей и строкой «ИТОГО», Листы 2–5 — детальные реестры по 4 отделам СУБД (`Жилищно-бытовой`, `Информационный`, `Корпоративный`, `Культурно-массовый`).
  - Маскирование персональных данных анонимных обращений («Аноним»).
- **Новый монолитный экран 2FA**:
  - Единый инпут с моноширинным шрифтом, умной фильтрацией и вставкой как сырых 6 цифр, так и полного сообщения из VK.
  - Таймер обратного отсчёта для повторной отправки в `sessionStorage`.
  - Автоматическая отправка формы при вводе 6 цифр.
- **Ограничение системного журнала («Журнал»)**:
  - Раздел `/logs/` и экспорт логов аудита в CSV доступны строго для роли `SUPERADMIN`.
- **Контроль синхронизации времени**:
  - Модуль `core/time_utils.py` и проверка дрейфа часов между сервером и СУБД PostgreSQL в `/health`.
- **Упрощение FAQ**:
  - Прозрачная модель: отдел (автор), конкретный вопрос, ответ.
- **Планы развития (Android App, Telegram Bot, Telegram Mini App для администраторов)**:
  - Разработан документ [docs/PLANS.md](docs/PLANS.md) с архитектурой мобильного рабочего места оператора и суперадминистратора: нативное приложение для Android (Kotlin, Jetpack Compose, Clean Architecture, Retrofit2, Room DB, FCM/RuStore пуши, биометрия) и Telegram Mini App (React, Tailwind, Telegram UI, строгая проверка прав администратора по HMAC initData). Обычные студенты используют публичный VK-бот.

### 8.11. Релиз v0.8.1: комплексный аудит безопасности, харденинг 2FA, i18n, индексы pg_trgm, отказоустойчивость и DR-скрипты
- **Кибербезопасность и защита старта**:
  - Внедрены `core/startup_guard.py` и `core/bootstrap_guard.py`: fail-closed валидация секретов при старте в `APP_ENV=production` (блокировка запуска со словарными/placeholder паролями и значениями короче 12 символов).
  - Глубокий харденинг 2FA (`web/routes/auth.py`, `web/security/otp_store.py`): лимиты попыток ввода (`WEB_ADMIN_2FA_MAX_ATTEMPTS = 5`) и повторных запросов (`WEB_ADMIN_2FA_RESEND_LIMIT = 3`), хранение OTP в Redis/памяти с хешированием Argon2id, возврат честных HTTP 401 и 429 (`Retry-After`), сохранение параметра `next` при 2FA редиректах.
- **Интернационализация (i18n)**:
  - Создан модуль `core/i18n.py` и конфигурация `babel.cfg`, интеграция с Jinja2 (`web/templating.py` через `install_gettext_callables`).
  - Устранена ошибка Jinja2 `UndefinedError` при рендеринге базового шаблона вне контекста запроса.
- **Оптимизация СУБД и производительность**:
  - Миграция `c4d5e6f7a8b9_add_pg_trgm_gin_indexes.py` с GIN-индексами расширения `pg_trgm` по тикетам и базе знаний.
  - Миграция `b5a7c9d1e2f3_enforce_fk_ondelete.py` с каскадными политиками `ON DELETE CASCADE / SET NULL`.
  - Фоновая очистка журнала rate-limit (`core/rate_limit_cleanup.py`).
- **Disaster Recovery (Резервное копирование и восстановление)**:
  - Разработаны кроссплатформенные PowerShell-скрипты `scripts/backup.ps1` и `scripts/restore.ps1` для Windows-окружения, актуализирован `scripts/restore.sh`.

### 8.12. Релиз v0.8.2: дизайн-система Glassmorphism & Rose Peony, редизайн навигации, аудит эмодзи, стабилизация 2FA и фикс модуля мероприятий
- **Дизайн-система Glassmorphism и палитра Rose Peony**:
  - Внедрён аутентичный эффект матового стекла (`backdrop-filter: blur(14px)` с fallback `-webkit-backdrop-filter`), полупрозрачные многослойные карточки (`--bg-glass-card`) и тонкие световые грани (`--border-glass`).
  - Разработана благородная светлая тема: мягкий шелковистый розовый градиент пиона (`#e85584` → `#cc3363`) с деликатным внутренним бликом `inset 0 1px 0 rgba(255, 255, 255, 0.22)` и рассеянной тенью вместо ядовитого неона `#ff2d87`, фарфорово-белая основа (`#fcf9fa`) и утончённая прозрачность фоновых пятен (`0.08–0.12`).
- **Строгий деловой стиль и аудит эмодзи**:
  - Полное удаление эмодзи из веб-панели управления, HTML-шаблонов, flash-сообщений и служебных скриптов.
  - Сохранение эмодзи строго в сообщениях VK-бота для создания комфортного и понятного тона общения со студентами.
- **Редизайн сайдбара и профиля оператора**:
  - Переработана карточка пользователя: аватар-сквиркл с первой буквой имени, значок роли (`Суперадмин`), аккуратные SVG-кнопки настроек и выхода.
  - Навигационные пункты переведены с псевдосимволов на векторные SVG-иконки.
  - Удалена дублирующая кнопка темы и скрыт мобильный крестик `✕` на десктопных разрешениях.
- **Стабилизация авторизации и 2FA**:
  - Устранён дедлок автоотправки формы в `2fa.html`, вызывавший зависание кнопки в состоянии «Проверка...».
  - Предотвращены циклические фоновые запросы в `store.js` при ответе 401 Unauthorized.
- **Модуль мероприятий (Events)**:
  - Устранено падение `AttributeError: type object 'Event' has no attribute 'is_active'` в VK-хендлере.
  - Реализована выборка предстоящих мероприятий по временной метке (`event_date >= now - timedelta(hours=3)`).
  - Добавлена поддержка отображения описания мероприятия и расширен словарь распознаваемых команд («Мероприятия», «События», «Афиша», «/events»).
  - Добавлен автоматический heartbeat-пинг и новые модульные тесты в `tests/test_dept_frame_and_events.py`.


